some commands are as follows:

git init
git remote add origin <-link->
git remote -v
git branch
git branch -M main
git push (-u  -->can be used)origin main
etc...

Also there are commands for branches

git branch
git checkout -b <Branch name>
git checkout -d <Branch name>

added comment in oldBr
added a comment in main branch
